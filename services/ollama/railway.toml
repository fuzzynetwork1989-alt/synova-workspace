[build]
builder = "NIXPACKS"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
# Configure Ollama to listen on Railway's injected PORT
OLLAMA_HOST = "0.0.0.0:$PORT"
