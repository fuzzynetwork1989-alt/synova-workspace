# Production Requirements - Zero Exit Code Issues
# Core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# Enhanced features
redis==5.0.1
aiofiles==23.2.1
psutil==5.9.6

# Optional dependencies with error handling
ollama==0.1.7 || echo "Ollama not available, continuing..."

# Development dependencies (optional)
pytest==7.4.3 || echo "Pytest not available, continuing..."
pytest-asyncio==0.21.1 || echo "Pytest-asyncio not available, continuing..."
